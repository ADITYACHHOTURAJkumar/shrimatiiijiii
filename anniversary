<!DOCTYPE html>
<html>
<head>
    <title>Happy Anniversary</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            text-align: center;
        }
        section {
            display: none;
            padding: 20px;
            background-color: #fef9e7;
            min-height: 100vh;
        }
        section.active {
            display: block;
        }
        button {
            padding: 10px 20px;
            font-size: 16px;
            margin: 10px;
            background-color: #f39c12;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        img {
            max-width: 50%;
            margin: 20px 0;
        }
    </style>
    <script>
        function showSection(sectionId) {
            document.querySelectorAll('section').forEach((section) => {
                section.classList.remove('active');
            });
            document.getElementById(sectionId).classList.add('active');
        }
    </script>
</head>
<body>
    <!-- Home Page -->
    <section id="home" class="active">
        <h1>Happy anniversary, [darling]!</h1>
        <img src="images/cover.jpg" alt="Cover Image">
        <p>Welcome to your special anniversary surprise!</p>
        <button onclick="showSection('question1')">Start</button>
    </section>

    <!-- Question 1 -->
    <section id="question1">
        <h2>Do you think you're the best gift in my life?</h2>
        <button onclick="showSection('question2')">Yes</button>
        <button onclick="showSection('question2')">No</button>
    </section>

    <!-- Question 2 -->
    <section id="question2">
        <h2>Do you love surprises?</h2>
        <button onclick="showSection('result')">Yes</button>
        <button onclick="showSection('result')">No</button>
    </section>

    <!-- Result -->
    <section id="result">
        <h1></h1>
     <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Anniversary!</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f0f0f0;
            overflow: hidden;
        }

        /* Video Background */
        video.background-video {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -1;
        }

        /* Container */
        .container {
            text-align: center;
            position: relative;
            z-index: 2;
            padding-top: 50px;
        }

        h1 {
            font-size: 48px;
            color: #ff69b4;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .love-image {
            margin-top: 20px;
            max-width: 350px;
            width: 100%;
            border-radius: 10px;
        }

        .cake-image {
            margin-top: 20px;
            max-width: 400px;
            width: 100%;
            border-radius: 10px;
        }

        .love-paragraph {
            font-size: 20px;
            color: #333;
            width: 70%;
            margin: 30px auto;
            line-height: 1.8;
            font-style: italic;
        }

        .balloon {
            position: absolute;
            width: 60px;
            height: 80px;
            background-color: #ff69b4;
            border-radius: 50%;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            animation: floatUp 7s infinite ease-in-out;
        }

        .balloon::after {
            content: "";
            position: absolute;
            bottom: -10px;
            left: 50%;
            width: 2px;
            height: 30px;
            background-color: #000;
            transform: translateX(-50%);
        }

        @keyframes floatUp {
            0% {
                transform: translateY(100vh);
            }
            100% {
                transform: translateY(-100vh);
            }
        }

        /* Balloon Color */
        .balloon:nth-child(odd) {
            background-color: #ff1493;
        }

        .balloon:nth-child(even) {
            background-color: #ff69b4;
        }

    </style>
</head>
<body>

    <!-- Video Background -->
    <video class="background-video" autoplay muted loop>
        <source src="https://www.w3schools.com/html/mov_bbb.mp4" type="video/mp4">
        Your browser does not support the video tag.
    </video>

    <!-- Content Container -->
    <div class="container">
        <h1>Happy Anniversary, My Love!</h1>

      
        <!-- Romantic Paragraph -->
        <p class="love-paragraph">
            Every day with you is an adventure filled with love, laughter, and unforgettable moments. 
            I cherish every second we’ve spent together, and I’m excited for all the beautiful days ahead. 
            Happy anniversary, my heart, my love, my everything. 💖
        </p>
    </div>

    <!-- Floating Balloons with String -->
    <script>
        // Function to create a floating balloon with a random position
        function createBalloon() {
            const balloon = document.createElement('div');
            balloon.classList.add('balloon');
            const randomX = Math.random() * window.innerWidth;
            const randomDelay = Math.random() * 4;

            balloon.style.left = ${randomX}px;
            balloon.style.animationDelay = -${randomDelay}s;

            document.body.appendChild(balloon);

            // Remove balloon after animation completes (7s)
            setTimeout(() => {
                balloon.remove();
            }, 7000);
        }

        // Continuously create balloons
        setInterval(createBalloon, 600);
    </script>

</body>
</html>

  <script>
    // Add animated floating hearts
    setInterval(() => {
      const heart = document.createElement('div');
      heart.innerHTML = '🩷';
      heart.style.position = 'absolute';
      heart.style.fontSize = '2em';
      heart.style.animation = 'rise 5s ease-out forwards';
      heart.style.left = Math.random() * window.innerWidth + 'px';
      heart.style.bottom = '0';
      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 5000);
    }, 500);

    const style = document.createElement('style');
    style.innerHTML = `
      @keyframes rise {
        0% { transform: translateY(0); opacity: 1; }
        100% { transform: translateY(-100vh); opacity: 0; }
      }
    `;
    document.head.appendChild(style);
  </script>
</body>
</html>   
        <img src="images/ending.jpg" alt="Ending Image">
    </section>
</body>
</html>
